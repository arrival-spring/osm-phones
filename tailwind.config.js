/** @type {import('tailwindcss').Config} */
module.exports = {
  // Scan all HTML files in the 'public' directory
  content: [
    "./public/**/*.html",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}