/** @type {import('tailwindcss').Config} */
module.exports = {
  // Scan all HTML files in the 'public' directory
  content: [
    "./public/**/*.html",
  ],
  darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [],
}